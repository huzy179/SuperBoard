# External Integrations

**Analysis Date:** 2026-03-19

## APIs & External Services

**Internal application APIs:**

- Nest REST API - consumed by web client service layer in `apps/web/lib/api-client.ts`, endpoint map in `apps/web/lib/api/endpoints`
  - SDK/Client: native `fetch` wrapper (`apiRequest`) in `apps/web/lib/api-client.ts`
  - Auth: bearer token from local storage in `apps/web/lib/auth-storage.ts`

**AI service surface:**

- FastAPI HTTP service (`/health`) in `apps/ai-service/main.py`
  - SDK/Client: not yet integrated from API/web runtime code
  - Auth: no auth mechanism implemented in current AI service endpoints
- gRPC contract defined in `apps/ai-service/proto/ai_service.proto`
  - Server bootstrap currently placeholder in `apps/ai-service/grpc_server.py`

**Planned/provider dependencies present:**

- OpenAI and Google Gemini SDKs declared in `apps/ai-service/requirements.txt`
  - Current runtime usage: not detected in `apps/ai-service/services/*.py` (stub implementations)

## Data Storage

**Databases:**

- PostgreSQL
  - Connection: `DATABASE_URL` (read by `apps/api/src/prisma/prisma.service.ts`)
  - Client: Prisma (`apps/api/prisma/schema.prisma`, migrations in `apps/api/prisma/migrations`)

**File Storage:**

- MinIO/S3-compatible storage documented for infra in `docker/README.md`
- Runtime storage client integration in app code: not detected
- Domain readiness exists via `Attachment` model in `apps/api/prisma/schema.prisma`

**Caching:**

- Redis (optional via feature flag)
  - Connection: `REDIS_URL` in `apps/api/src/common/redis.service.ts`
  - Queue integration: BullMQ in `apps/api/src/common/queue.service.ts`

## Authentication & Identity

**Auth Provider:**

- Current runtime: custom JWT auth (Nest guard + bearer token)
  - Implementation: `apps/api/src/modules/auth/auth.service.ts` and `apps/api/src/common/guards/bearer-auth.guard.ts`
- Keycloak variables are accepted by env schema (`apps/api/src/config/env.ts`) but provider calls are not implemented in active auth flow

## Monitoring & Observability

**Error Tracking:**

- Dedicated external error tracking service (Sentry/Datadog/etc): none detected

**Logs:**

- Structured JSON logging via Pino in `apps/api/src/common/logger.ts`
- Unified HTTP error envelope via filter in `apps/api/src/common/filters/http-exception.filter.ts`
- Service health checks in `apps/api/src/health.controller.ts` and `scripts/health-check.mjs`

## CI/CD & Deployment

**Hosting:**

- Deployment targets not fixed yet (placeholder steps in `.github/workflows/cd.yml`)

**CI Pipeline:**

- GitHub Actions CI runs lint/typecheck/test in `.github/workflows/ci.yml`
- CD uses path-based change detection with `dorny/paths-filter` in `.github/workflows/cd.yml`

## Environment Configuration

**Required env vars:**

- API core: `DATABASE_URL`, `REDIS_URL`, `FRONTEND_URL`, `AI_SERVICE_URL`, `JWT_SECRET` in `apps/api/src/config/env.ts`
- Optional infra/provider knobs: `ENABLE_REDIS`, `ENABLE_QUEUE`, `ELASTICSEARCH_URL`, `KEYCLOAK_*` in `apps/api/src/config/env.ts`
- Web client base URL: `NEXT_PUBLIC_API_URL` consumed in `apps/web/lib/api-client.ts`

**Secrets location:**

- Local development: `.env.local` files generated by `scripts/setup.mjs` (per app)
- CI/CD: GitHub Actions Secrets referenced in `.github/workflows/cd.yml`

## Webhooks & Callbacks

**Incoming:**

- No webhook callback controllers detected in `apps/api/src` (only `health`, `auth`, `projects` routes)

**Outgoing:**

- No outbound webhook dispatch integration detected in current runtime code

---

_Integration audit: 2026-03-19_
