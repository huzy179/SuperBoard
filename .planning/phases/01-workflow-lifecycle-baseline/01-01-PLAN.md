---
phase: 01-workflow-lifecycle-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/<timestamp>_phase1_lifecycle_baseline/migration.sql
  - apps/api/src/modules/workspace/workspace.module.ts
  - apps/api/src/modules/workspace/workspace.controller.ts
  - apps/api/src/modules/workspace/workspace.service.ts
  - apps/api/src/modules/task/task.module.ts
  - apps/api/src/modules/task/task.controller.ts
  - apps/api/src/modules/task/task.service.ts
  - apps/api/src/modules/project/project.service.ts
  - apps/api/src/app.module.ts
  - packages/shared/src/dtos/project.dto.ts
  - packages/shared/src/dtos/workflow.dto.ts
  - apps/api/test/workflow-lifecycle.integration.test.ts
  - apps/api/package.json
autonomous: true
requirements:
  - WFLO-01
must_haves:
  truths:
    - 'User có thể create/read/update/archive workspace, project, task qua API với cùng semantics soft-delete + restore.'
    - 'User không thể restore task/project khi parent đang archived và nhận hướng dẫn restore theo thứ tự.'
    - 'Danh sách mặc định ẩn archived items và chỉ hiện khi bật showArchived.'
  artifacts:
    - path: 'apps/api/src/modules/workspace/workspace.controller.ts'
      provides: 'Workspace lifecycle endpoints (CRUD + archive/restore + showArchived).'
    - path: 'apps/api/src/modules/task/task.controller.ts'
      provides: 'Task archive/restore endpoint với parent invariant check.'
    - path: 'apps/api/prisma/schema.prisma'
      provides: 'Lifecycle persistence fields và relation constraints cho workspace/project/task.'
  key_links:
    - from: 'apps/api/src/modules/workspace/workspace.controller.ts'
      to: 'apps/api/src/modules/workspace/workspace.service.ts'
      via: 'controller delegates lifecycle operations'
      pattern: "this\\.workspaceService\\.(create|update|archive|restore)"
    - from: 'apps/api/src/modules/task/task.service.ts'
      to: 'apps/api/prisma/schema.prisma'
      via: 'restore guard reads parent archive flags before update'
      pattern: 'Cannot restore.*parent.*archived|isArchived|deletedAt'
---

<objective>
Hoàn thiện lifecycle backend cho workspace/project/task theo đúng soft-delete/restore semantics và parent invariant để đáp ứng WFLO-01.

Purpose: Tạo nền tảng domain ổn định trước khi mở rộng status customization và optimistic consistency ở plan sau.
Output: Module workspace/task hoạt động, project lifecycle được chuẩn hóa, migration/schema + DTO contracts đồng bộ và có integration test scaffold.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workflow-lifecycle-baseline/01-CONTEXT.md
@.planning/phases/01-workflow-lifecycle-baseline/01-RESEARCH.md
@apps/api/src/modules/project/project.service.ts
@packages/shared/src/dtos/project.dto.ts

<interfaces>
From packages/shared/src/dtos/project.dto.ts:
```typescript
export interface ProjectTaskItemDTO {
  id: string;
  title: string;
  status: 'todo' | 'in_progress' | 'in_review' | 'done' | 'cancelled';
  priority: 'low' | 'medium' | 'high' | 'urgent';
}
```

From apps/api/src/modules/project/project.controller.ts:

```typescript
@Delete(':projectId')
async deleteProject(...): Promise<DeleteProjectResponseDTO>;

@Patch(':projectId/tasks/:taskId/status')
async updateTaskStatus(...): Promise<UpdateTaskStatusResponseDTO>;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish lifecycle contracts and verification baseline (Wave 0)</name>
  <read_first>
    - .planning/phases/01-workflow-lifecycle-baseline/01-VALIDATION.md
    - apps/api/package.json
    - packages/shared/src/dtos/project.dto.ts
  </read_first>
  <files>
    - packages/shared/src/dtos/workflow.dto.ts
    - packages/shared/src/dtos/project.dto.ts
    - apps/api/test/workflow-lifecycle.integration.test.ts
    - apps/api/package.json
  </files>
  <action>
    Create shared lifecycle DTO contracts for workspace/project/task archive/restore and showArchived filtering; keep backward compatibility by retaining current project DTO exports.
    Add API test script if missing and create integration test scaffold `workflow-lifecycle.integration.test.ts` covering create/read/update/archive/restore flows and parent-restore blocking cases.
    Do not implement business logic inside tests yet; keep tests executable and focused on endpoint behavior contracts.
  </action>
  <verify>
    <automated>npm --workspace @superboard/api run test -- workflow-lifecycle.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export interface .*Archive.*RequestDTO" packages/shared/src/dtos/workflow.dto.ts` returns at least 2 matches.
    - `grep -n "workflow-lifecycle.integration.test.ts" apps/api/package.json` returns test script reference or include pattern.
    - `grep -n "describe('workflow lifecycle'" apps/api/test/workflow-lifecycle.integration.test.ts` returns 1+ match.
  </acceptance_criteria>
  <done>
    Lifecycle contracts exist in shared package, API test command is runnable, and integration test scaffold exists for WFLO-01 behavior sampling.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement workspace/task lifecycle modules with soft-delete + restore invariants</name>
  <read_first>
    - apps/api/src/modules/project/project.service.ts
    - apps/api/prisma/schema.prisma
    - .planning/phases/01-workflow-lifecycle-baseline/01-CONTEXT.md
  </read_first>
  <files>
    - apps/api/prisma/schema.prisma
    - apps/api/prisma/migrations/<timestamp>_phase1_lifecycle_baseline/migration.sql
    - apps/api/src/modules/workspace/workspace.module.ts
    - apps/api/src/modules/workspace/workspace.controller.ts
    - apps/api/src/modules/workspace/workspace.service.ts
    - apps/api/src/modules/task/task.module.ts
    - apps/api/src/modules/task/task.controller.ts
    - apps/api/src/modules/task/task.service.ts
    - apps/api/src/modules/project/project.service.ts
    - apps/api/src/app.module.ts
  </files>
  <behavior>
    - Test 1: archive workspace/project/task sets `isArchived=true` and `deletedAt!=null`.
    - Test 2: restore child while parent archived returns 400 with guidance message.
    - Test 3: list endpoints exclude archived by default and include archived only with `showArchived=true`.
  </behavior>
  <action>
    Implement workspace and task Nest modules (controller/service/module) using existing project module patterns.
    Add/adjust schema and migration to support lifecycle invariants and query filters aligned with current soft-delete model.
    Refactor project lifecycle service paths to use explicit archive semantics (no hard delete), and ensure restore path validates parent relation ordering.
    Register new modules in app module and keep error shape consistent with `apiSuccess` + global exception filter.
  </action>
  <verify>
    <automated>npm --workspace @superboard/api run test -- workflow-lifecycle.integration.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@Controller('workspaces')" apps/api/src/modules/workspace/workspace.controller.ts` returns 1 match.
    - `grep -n "@Controller('tasks')" apps/api/src/modules/task/task.controller.ts` returns 1 match.
    - `grep -n "Cannot restore.*restore.*first" apps/api/src/modules/task/task.service.ts` returns 1+ match.
    - `grep -n "WorkspaceModule\|TaskModule" apps/api/src/app.module.ts` returns 2+ matches.
  </acceptance_criteria>
  <done>
    Workspace/project/task lifecycle API supports create/read/update/archive/restore with parent-order restore guard and default archived filtering.
  </done>
</task>

</tasks>

<verification>
- API lifecycle integration test scaffold and command run green.
- Archive/restore invariants validated via automated tests (including parent-first restore ordering).
- `npm --workspace @superboard/api run lint` reports no newly introduced lint errors in modified modules.
</verification>

<success_criteria>

- WFLO-01 backend contract exists and is enforceable through endpoints + tests.
- Workspace/task modules are no longer empty and lifecycle behavior is consistent with project module.
- Archived visibility semantics (`showArchived`) are explicit and test-covered.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-lifecycle-baseline/01-01-SUMMARY.md`
</output>
