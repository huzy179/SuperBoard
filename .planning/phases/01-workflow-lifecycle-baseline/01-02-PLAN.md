---
phase: 01-workflow-lifecycle-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/<timestamp>_phase1_status_model/migration.sql
  - apps/api/src/modules/workflow/workflow.module.ts
  - apps/api/src/modules/workflow/workflow.controller.ts
  - apps/api/src/modules/workflow/workflow.service.ts
  - apps/api/src/modules/project/project.service.ts
  - apps/api/src/modules/task/task.service.ts
  - packages/shared/src/dtos/project.dto.ts
  - packages/shared/src/dtos/workflow.dto.ts
autonomous: true
requirements:
  - WFLO-02
must_haves:
  truths:
    - 'User có thể cấu hình status schema theo workspace/project theo mô hình template + project override.'
    - 'Task transition chỉ đi theo strict transition graph và trả lỗi nhất quán khi transition invalid/stale.'
    - 'Project creation luôn clone status template thành snapshot độc lập cho project mới.'
  artifacts:
    - path: 'apps/api/src/modules/workflow/workflow.service.ts'
      provides: 'Status schema CRUD, category mapping và transition policy enforcement.'
    - path: 'apps/api/src/modules/project/project.service.ts'
      provides: 'Project create flow clone workspace template snapshot.'
    - path: 'packages/shared/src/dtos/workflow.dto.ts'
      provides: 'Shared workflow status contracts cho API/Web integration.'
  key_links:
    - from: 'apps/api/src/modules/workflow/workflow.service.ts'
      to: 'apps/api/src/modules/task/task.service.ts'
      via: 'task status update validates transition graph before persist'
      pattern: 'assertTransitionAllowed|validateTransition'
    - from: 'apps/api/src/modules/project/project.service.ts'
      to: 'apps/api/src/modules/workflow/workflow.service.ts'
      via: 'project create loads workspace template and persists project snapshot'
      pattern: 'clone.*template|create.*workflow'
---

<objective>
Triển khai backend custom status model theo workspace/project với strict transition graph để đáp ứng phần domain của WFLO-02.

Purpose: Khóa chặt state model và transition invariant ở API trước khi wire optimistic UI consistency ở plan kế tiếp.
Output: Workflow status module + API/shared contracts + transition guard + migration schema.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workflow-lifecycle-baseline/01-CONTEXT.md
@.planning/phases/01-workflow-lifecycle-baseline/01-RESEARCH.md
@.planning/phases/01-workflow-lifecycle-baseline/01-01-SUMMARY.md
@apps/api/src/modules/workflow/workflow.service.ts

<interfaces>
From packages/shared/src/dtos/project.dto.ts:
```typescript
export interface UpdateTaskStatusRequestDTO {
  status: 'todo' | 'in_progress' | 'in_review' | 'done' | 'cancelled';
}
```

From apps/web/lib/services/project-service.ts:

```typescript
export async function updateProjectTaskStatus(
  projectId: string,
  taskId: string,
  payload: UpdateTaskStatusRequestDTO,
): Promise<ProjectTaskItemDTO>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement workflow status domain (template + override + strict transitions)</name>
  <read_first>
    - .planning/phases/01-workflow-lifecycle-baseline/01-CONTEXT.md
    - apps/api/prisma/schema.prisma
    - apps/api/src/modules/project/project.service.ts
    - packages/shared/src/dtos/project.dto.ts
  </read_first>
  <files>
    - apps/api/prisma/schema.prisma
    - apps/api/prisma/migrations/<timestamp>_phase1_status_model/migration.sql
    - apps/api/src/modules/workflow/workflow.module.ts
    - apps/api/src/modules/workflow/workflow.controller.ts
    - apps/api/src/modules/workflow/workflow.service.ts
    - apps/api/src/modules/project/project.service.ts
    - apps/api/src/modules/task/task.service.ts
    - packages/shared/src/dtos/workflow.dto.ts
    - packages/shared/src/dtos/project.dto.ts
  </files>
  <behavior>
    - Test 1: project creation clones workspace status template snapshot (no dynamic link).
    - Test 2: update task status outside transition graph returns 400 and keeps current status.
    - Test 3: status delete/rename in-use requires migration of affected tasks first.
  </behavior>
  <action>
    Add workflow module to own status schema lifecycle with required canonical categories (`todo`, `in_progress`, `in_review`, `done`, `blocked`) and role checks per locked decision.
    Ensure project creation clones workspace template statuses at create-time and stores project override snapshot.
    Enforce strict transition graph in task status mutations and block destructive status edits when tasks still reference old status unless migration payload is provided.
    Keep backward compatibility for legacy `cancelled` reads while normalizing new writes to decision-compliant taxonomy.
  </action>
  <verify>
    <automated>npm --workspace @superboard/api run test -- workflow-status-policy -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "blocked" apps/api/src/modules/workflow/workflow.service.ts` returns canonical category usage.
    - `grep -n "clone.*workspace.*template" apps/api/src/modules/project/project.service.ts` returns 1+ match.
    - `grep -n "Invalid transition" apps/api/src/modules/task/task.service.ts` returns 1+ match.
  </acceptance_criteria>
  <done>
    Workflow status domain supports workspace template + project override with strict transition enforcement and safe migration constraints.
  </done>
</task>

</tasks>

<verification>
- Status policy automated tests pass for strict transition, template cloning, and in-use migration guard.
- `npm --workspace @superboard/api run lint` shows no new lint regressions in touched files.
</verification>

<success_criteria>

- WFLO-02 backend domain is implementable with explicit status model ownership and invariant enforcement.
- Status taxonomy no longer relies on hard-coded workflow enum as sole source of truth.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-lifecycle-baseline/01-02-SUMMARY.md`
</output>
