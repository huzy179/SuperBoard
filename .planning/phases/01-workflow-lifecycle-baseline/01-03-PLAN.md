---
phase: 01-workflow-lifecycle-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - apps/web/lib/services/project-service.ts
  - apps/web/app/(private)/jira/projects/[projectId]/page.tsx
  - apps/web/hooks/use-project-list.ts
  - apps/web/app/(private)/jira/page.tsx
  - apps/web/test/jira-status-consistency.test.tsx
autonomous: true
requirements:
  - WFLO-01
  - WFLO-02
must_haves:
  truths:
    - 'User có thể create/read/update/archive project/task từ UI và phản ánh đúng với API lifecycle semantics.'
    - 'Board/list hiển thị cùng trạng thái sau update, refresh và focus-revalidate.'
    - 'Optimistic update rollback đúng khi gặp stale/conflict và cho phép retry.'
    - 'Archived items mặc định ẩn và chỉ hiện khi bật Show archived.'
  artifacts:
    - path: 'apps/web/lib/services/project-service.ts'
      provides: 'Client contract cho status transition/conflict handling.'
    - path: 'apps/web/app/(private)/jira/projects/[projectId]/page.tsx'
      provides: 'Optimistic-first status update + stale conflict rollback/retry UX.'
    - path: 'apps/web/app/(private)/jira/page.tsx'
      provides: 'Project/task lifecycle controls (archive/restore/showArchived) ở màn hình tổng quan.'
    - path: 'apps/web/hooks/use-project-list.ts'
      provides: 'Shared revalidation path giữ board/list đồng bộ dữ liệu.'
  key_links:
    - from: 'apps/web/app/(private)/jira/projects/[projectId]/page.tsx'
      to: 'apps/web/lib/services/project-service.ts'
      via: 'optimistic mutation + conflict retry'
      pattern: 'updateProjectTaskStatus|setProject\(previous\)|retry'
    - from: 'apps/web/app/(private)/jira/projects/[projectId]/page.tsx'
      to: 'apps/web/hooks/use-project-list.ts'
      via: 'focus/manual refresh revalidate'
      pattern: 'revalidate|visibilitychange|focus'
---

<objective>
Wire optimistic-first UI consistency cho board/list sau khi backend status domain đã ổn định ở 01-02, đồng thời hoàn tất phần trải nghiệm lifecycle UI (archive/restore/showArchived) cho WFLO-01.

Purpose: Đảm bảo thao tác trạng thái nhanh nhưng vẫn nhất quán và recoverable khi xảy ra stale/conflict, đồng thời phản ánh đúng semantics lifecycle từ API lên UI.
Output: Web service/hook/page được đồng bộ logic optimistic + rollback/retry + archived toggle + lifecycle UI controls, kèm consistency test.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workflow-lifecycle-baseline/01-CONTEXT.md
@.planning/phases/01-workflow-lifecycle-baseline/01-UI-SPEC.md
@.planning/phases/01-workflow-lifecycle-baseline/01-02-SUMMARY.md
@apps/web/app/(private)/jira/projects/[projectId]/page.tsx
@apps/web/lib/services/project-service.ts

<interfaces>
From apps/web/lib/services/project-service.ts:
```typescript
export async function updateProjectTaskStatus(
  projectId: string,
  taskId: string,
  payload: UpdateTaskStatusRequestDTO,
): Promise<ProjectTaskItemDTO>;
```

From apps/web/hooks/use-project-list.ts:

```typescript
export function useProjectList(...): {
  projects: ProjectListItemDTO[];
  revalidate: () => Promise<void>;
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align web service + hook contracts for optimistic consistency flow</name>
  <read_first>
    - .planning/phases/01-workflow-lifecycle-baseline/01-UI-SPEC.md
    - apps/web/lib/services/project-service.ts
    - apps/web/hooks/use-project-list.ts
    - packages/shared/src/dtos/project.dto.ts
  </read_first>
  <files>
    - apps/web/lib/services/project-service.ts
    - apps/web/hooks/use-project-list.ts
  </files>
  <action>
    Update project service APIs to normalize stale/conflict payload from backend and expose a retry-friendly error contract to UI callers.
    Ensure project list hook exposes deterministic revalidate behavior for mutation success/focus/manual refresh paths without duplicate fetch storms.
    Keep API-client error shape compatibility and avoid introducing new global state dependencies.
  </action>
  <verify>
    <automated>npm --workspace @superboard/web run test -- jira-status-consistency.test.tsx -t "service contract"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "stale\|conflict" apps/web/lib/services/project-service.ts` returns normalized conflict handling.
    - `grep -n "revalidate" apps/web/hooks/use-project-list.ts` returns exported revalidate path.
    - `grep -n "retry" apps/web/lib/services/project-service.ts` returns retry-friendly error metadata.
  </acceptance_criteria>
  <done>
    Service/hook layer provides stable contracts for optimistic mutation, rollback trigger, and deterministic data refresh.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement page-level optimistic rollback/retry UX + archived visibility consistency</name>
  <read_first>
    - .planning/phases/01-workflow-lifecycle-baseline/01-UI-SPEC.md
    - apps/web/app/(private)/jira/projects/[projectId]/page.tsx
    - apps/web/app/(private)/jira/page.tsx
    - apps/web/test/jira-status-consistency.test.tsx
  </read_first>
  <files>
    - apps/web/app/(private)/jira/projects/[projectId]/page.tsx
    - apps/web/app/(private)/jira/page.tsx
    - apps/web/test/jira-status-consistency.test.tsx
  </files>
  <action>
    Wire optimistic-first status mutation in project page, with rollback to previous snapshot when stale/conflict is returned, plus explicit warning banner and retry control.
    Add `Show archived` toggle default-off and keep board/list query behavior synchronized with service/hook revalidate semantics.
    Expose create/read/update/archive/restore UI controls for project/task scope in Jira views and ensure API lifecycle semantics are reflected consistently after mutation + refresh.
    Extend consistency test to assert parity after transition, refresh/focus, and conflict rollback-retry flow.
  </action>
  <verify>
    <automated>npm --workspace @superboard/web run test -- jira-status-consistency.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Show archived" "apps/web/app/(private)/jira/projects/[projectId]/page.tsx"` returns 1+ match.
    - `grep -n "archive\|restore" "apps/web/app/(private)/jira/page.tsx"` returns lifecycle control handlers.
    - `grep -n "Không thể cập nhật trạng thái task\. Dữ liệu có thể đã thay đổi" "apps/web/app/(private)/jira/projects/[projectId]/page.tsx"` returns 1+ match.
    - `grep -n "visibilitychange\|focus" "apps/web/app/(private)/jira/projects/[projectId]/page.tsx"` returns auto-refresh hook logic.
  </acceptance_criteria>
  <done>
    Board/list/status interactions stay consistent after mutation and refresh; stale conflicts are recoverable with clear user feedback and retry path.
  </done>
</task>

</tasks>

<verification>
- Web consistency test validates optimistic rollback + conflict messaging + archived visibility toggling.
- `npm --workspace @superboard/web run lint` shows no new lint regressions in touched files.
</verification>

<success_criteria>

- WFLO-01 UI lifecycle controls phản ánh đúng semantics create/read/update/archive/restore từ API ở phạm vi project/task.
- WFLO-02 UI behavior matches approved interaction contract in UI-SPEC for optimistic-first and conflict recovery.
- Board/list consistency remains stable across manual refresh and app focus revalidation.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-lifecycle-baseline/01-03-SUMMARY.md`
</output>
