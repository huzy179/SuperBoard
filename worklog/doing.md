# DOING

## Đang tập trung

- [ ] MVP 2: Jira-first implementation sau khi đã chốt xong base standardization (global auth guard + response envelope + shared DTO contracts).

## Blockers hiện tại

- Không còn blocker.

## Next actions ngay

1. Implement `POST /projects` + form tạo project ở FE dùng contract mới.
2. Implement `Task` CRUD cơ bản và danh sách task theo project.
3. Bổ sung API client layer FE (fetch wrapper + auth/error handling) để giảm lặp logic ở page components.
